name: wt_firepod
description: Package the integrates Firebase and Riverpod functionality.
version: 0.0.1
publish_to: 'none'
homepage: https://github.com/tmcmaster/wt_firepod

environment:
  sdk: '>=2.18.2 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  wt_models:
    git: git@github.com:tmcmaster/wt_models.git
  wt_settings:
    git: git@github.com:tmcmaster/wt_settings.git

  shared_preferences:
  crypto:
  logger:
  riverpod: ^2.0.2
  freezed_annotation:
  font_awesome_flutter:
  flutter_riverpod:
  hooks_riverpod:
  firebase_core: ^2.1.1
  firebase_auth: ^4.1.0
  firebase_database: ^10.0.3
  firebase_remote_config: ^3.0.3
  cloud_functions: ^4.0.3
  #  google_sign_in: ^5.4.2
  #  sign_in_with_apple: ^4.1.0
  firebase_ui_auth: ^1.0.3
  firebase_ui_oauth_google: ^1.0.3
  firebase_ui_oauth_apple: ^1.0.3
  firebase_ui_oauth_facebook: ^1.0.3
  firebase_ui_oauth_twitter: ^1.0.3
  firebase_ui_database: ^1.0.3
  flutter_form_builder:
  form_builder_validators:
  flutter_animated_dialog:
  uuid:
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
