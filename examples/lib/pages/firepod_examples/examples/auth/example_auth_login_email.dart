import 'package:flutter/material.dart';

class ExampleFirepodLoginEmail extends StatelessWidget {
  const ExampleFirepodLoginEmail({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [

      ],
    );
  }
}
